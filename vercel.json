{
  "buildCommand": "npm cache clean --force && npm ci --legacy-peer-deps && npm run build",
  "installCommand": "npm cache clean --force && npm ci --legacy-peer-deps",
  "framework": "nextjs",
  "functions": {
    "src/app/api/**/*.ts": {
      "maxDuration": 30
    }
  },
  "env": {
    "NODE_ENV": "production",
    "NODE_DEBUG": "http,https,net"
  },
  "build": {
    "env": {
      "NODE_ENV": "production",
      "NODE_DEBUG": "http,https,net"
    }
  },
  "ignoreCommand": "git diff --quiet HEAD^ HEAD ./"
}
