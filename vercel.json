{
  "buildCommand": "rm -rf .next && npm run build",
  "installCommand": "npm install --legacy-peer-deps --force",
  "framework": "nextjs",
  "functions": {
    "src/app/api/**/*.ts": {
      "maxDuration": 30
    }
  },
  "env": {
    "NODE_ENV": "production",
    "HUSKY": "0"
  },
  "build": {
    "env": {
      "NODE_ENV": "production",
      "HUSKY": "0"
    }
  }
}
